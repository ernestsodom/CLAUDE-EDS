# ─── Mercado Público (licitaciones) ─────────────────────────────
# Ticket gratuito de la API oficial. Obtenlo en el portal de
# desarrolladores: https://api.mercadopublico.cl/modules/api/
# Si lo dejas vacío se usa un ticket de demostración con cuota muy limitada.
MP_TICKET=

# Opcionales (tienen valores por defecto razonables):
# MP_DATA_DIR=./data
# MP_TIMEOUT=30
# MP_MAX_RETRIES=4
# MP_REQUEST_DELAY=0.5
# MP_CACHE_TTL_HOURS=12
